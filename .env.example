# Plaid API Credentials
# Get these from https://dashboard.plaid.com/developers/keys
PLAID_CLIENT_ID=6a5ed0e97816a0000cf020d8
PLAID_SECRET=e42ccc4e0c216b30b00384df493b54

# Plaid environment: sandbox (testing), production (real banks)
PLAID_ENV=sandbox

# Supported country codes (comma-separated)
PLAID_COUNTRY_CODES=CA

# Webhook URL - must be publicly accessible via HTTPS
# This is where Plaid sends notifications when new transactions are available
PLAID_WEBHOOK_URL=https://finance.onlinewebapps.com/plaid/webhook
